general:
  artifacts:
    - "target/"
machine:
  java:
    version: openjdk8
  services:
    - docker

deployment:
  hub:
    branch: master
    commands:
      - docker info
      - docker build -t ryanjbaxter/ocr-eureka .
